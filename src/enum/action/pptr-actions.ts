/**
 * Enum des actions Puppeteer
 */
export enum EPptrAction {
  PPTR = 'pptr',
  GOTO = 'goto*',
  VIEWPORT = 'viewport*',
  WAIT_FOR_SELECTOR = 'wait-for-selector*',
  NAVIGATION = 'navigation*',
  NAVIGATION_PROMISE = 'navigation-promise*',
  FRAME_SET = 'frame-set*'
}
